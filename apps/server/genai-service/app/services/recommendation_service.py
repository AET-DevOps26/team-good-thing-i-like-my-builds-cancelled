from typing import List


def recommend_destinations(visited: List[str]) -> List[str]:
    """Return placeholder recommendations based on visited places."""
    return [f"Recommendation for {place}" for place in visited]
