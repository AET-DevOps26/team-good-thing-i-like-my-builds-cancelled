{{- if .Values.monitoring.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: {{ .Values.monitoring.namespace }}
  labels:
    app: prometheus
data:
  prometheus.yml: |
{{ tpl (.Files.Get "files/prometheus/prometheus.yml") . | indent 4 }}
{{- end }}
