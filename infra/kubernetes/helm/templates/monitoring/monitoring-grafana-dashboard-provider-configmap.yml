{{- if .Values.monitoring.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
  namespace: {{ .Values.monitoring.namespace }}
  labels:
    app: grafana
data:
  dashboards.yml: |
{{ tpl (.Files.Get "files/grafana/dashboards.yml") . | indent 4 }}
{{- end }}
