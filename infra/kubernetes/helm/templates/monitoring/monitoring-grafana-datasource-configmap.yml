{{- if .Values.monitoring.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: {{ .Values.monitoring.namespace }}
  labels:
    app: grafana
data:
  datasources.yml: |
{{ tpl (.Files.Get "files/grafana/datasources.yml") . | indent 4 }}
{{- end }}
