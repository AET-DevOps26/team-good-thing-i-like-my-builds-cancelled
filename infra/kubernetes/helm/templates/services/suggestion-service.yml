---
apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels:
    app: suggestion
spec:
  selector:
    app: suggestion
  ports:
    - protocol: TCP
      port: 9300
      targetPort: suggestion-api
