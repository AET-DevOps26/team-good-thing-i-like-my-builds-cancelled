---
apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels:
    app: suggestion
spec:
  selector:
    app: suggestion
  ports:
    - protocol: TCP
      port: 8080
      targetPort: suggestion-api
