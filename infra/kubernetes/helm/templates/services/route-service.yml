---
apiVersion: v1
kind: Service
metadata:
  name: route-service
  labels:
    app: route
    monitoring: "true"
spec:
  selector:
    app: route
  ports:
    - protocol: TCP
      port: 80
      targetPort: route-api
