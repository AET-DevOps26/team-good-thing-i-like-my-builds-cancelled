apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: prometheus
    url: http://prometheus-service.{{ .Values.monitoring.namespace }}.svc.cluster.local:9090/monitoring/prometheus/
    isDefault: true
    editable: false
