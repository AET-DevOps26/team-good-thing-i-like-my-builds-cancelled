---
apiVersion: v1
kind: Service
metadata:
  name: route-service
  namespace: my-namespace
  labels:
    app: route
spec:
  selector:
    app: route
  ports:
    - protocol: TCP
      port: 80
      targetPort: route-api
